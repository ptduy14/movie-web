/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['img.ophim.live'], // Thêm domain img.ophim.live
  },
};

export default nextConfig;
